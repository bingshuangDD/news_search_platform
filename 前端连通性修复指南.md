# 前端连通性修复指南

## 项目架构

```
新闻头条项目/
├── backend/                  # FastAPI 后端 (Python)
│   ├── app/
│   │   ├── main.py           # 入口，端口 8000
│   │   ├── config/database.py # MySQL 异步连接
│   │   ├── models/news.py    # ORM 模型
│   │   ├── crud/news.py      # 数据库操作
│   │   ├── routers/news.py   # API 路由 /api/news/*
│   │   └── schemas.py        # Pydantic 校验
│   ├── venv/                 # Python 虚拟环境
│   └── requirements.txt
│
└── frontend/                 # Vue3 + Vite 前端
    ├── src/
    │   ├── main.js           # 入口
    │   ├── config/api.js     # API 地址配置 → http://127.0.0.1:8000
    │   ├── router/           # 路由
    │   ├── store/            # Pinia 状态管理
    │   ├── views/            # 页面组件
    │   └── i18n/             # 国际化
    ├── vite.config.js
    └── package.json
```

**前端 → 后端连通方式**：前端通过 `axios` 直接请求 `http://127.0.0.1:8000`，配置文件在 [src/config/api.js](frontend/src/config/api.js)。

---

## 一、环境要求

| 组件 | 版本 | 说明 |
|------|------|------|
| Node.js | >= 18 | 前端运行环境 |
| Python | >= 3.10 | 后端运行环境 |
| MySQL | 8.0 | 数据库，需创建 `news_app` 库 |

---

## 二、前端 npm install 权限错误（EPERM）

### 错误表现

```
npm error code EPERM
npm error syscall open
npm error path D:\Vue\jsnode\node_cache\_cacache\tmp\...
npm error [Error: EPERM: operation not permitted]
```

### 根本原因

npm 缓存目录（`D:\Vue\jsnode\node_cache`）被 Windows Defender 或杀毒软件锁定，导致 npm 无法读写临时文件。

### 解决步骤

**1. 删除损坏的 node_modules**

```bash
# 使用 cmd 执行（bash 的 rm 可能因权限失败）
cmd.exe /c "rmdir /s /q D:\Fastapi+Vue-新闻头条项目\frontend\node_modules"
```

**2. 将 npm 缓存迁移到用户目录**

```bash
# 从有问题的 D 盘自定义路径 → 系统用户目录
npm config set cache "C:\Users\hp\AppData\Local\npm-cache"
```

**3. 重新安装**

```bash
cd frontend
npm install
```

### 如果仍然失败

- **关闭 VS Code 后重试**：VS Code 可能锁定了 node_modules 中的文件
- **暂时关闭 Windows Defender 实时保护**：设置 → 隐私和安全性 → Windows 安全中心 → 病毒和威胁防护 → 管理设置 → 关闭实时保护
- **以管理员身份运行终端**：右键终端 → 以管理员身份运行

---

## 三、后端启动

### 1. 创建数据库

```sql
CREATE DATABASE IF NOT EXISTS news_app DEFAULT CHARSET utf8mb4;
```

### 2. 配置数据库连接

编辑 [backend/app/config/database.py](backend/app/config/database.py)：

```python
# MySQL 用户名:密码@地址:端口/数据库
ASYNC_DATABASE_URL = "mysql+aiomysql://root:123456@localhost:3306/news_app?charset=utf8"
```

### 3. 激活虚拟环境并安装依赖

```bash
cd backend
python -m venv venv                    # 创建虚拟环境（首次）
venv\Scripts\activate                  # Windows 激活
pip install -r requirements.txt        # 安装依赖
```

### 4. 启动服务

```bash
uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```

启动后访问：
- API 文档：http://127.0.0.1:8000/docs
- 健康检查：http://127.0.0.1:8000/health

---

## 四、前端启动

```bash
cd frontend
npm run dev
```

Vite 默认在 `http://localhost:5173` 启动。

---

## 五、前后端连通验证

### 连通架构

```
浏览器 (localhost:5173)
    │
    ├── 页面渲染：Vite 开发服务器
    │
    └── API 请求：axios → http://127.0.0.1:8000/api/news/...
                           │
                           └── FastAPI 后端 (端口 8000)
                                   │
                                   └── MySQL (端口 3306)
```

### 验证步骤

**1. 确认后端运行正常**

```bash
curl http://127.0.0.1:8000/health
# 返回: {"status":"healthy"}
```

**2. 确认 API 可访问**

```bash
curl http://127.0.0.1:8000/api/news/categories
# 返回: {"code":200,"message":"获取分类成功","data":[...]}
```

**3. 确认前端页面加载**

浏览器打开 `http://localhost:5173`，打开 F12 控制台 → Network 标签，查看 API 请求是否返回 200。

### 常见跨域问题（CORS）

如果浏览器控制台出现 `Access-Control-Allow-Origin` 错误，需要在后端 [backend/app/main.py](backend/app/main.py) 中添加 CORS 中间件：

```python
from fastapi.middleware.cors import CORSMiddleware

app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:5173"],
    allow_methods=["*"],
    allow_headers=["*"],
)
```

> 当前开发环境下（同为 127.0.0.1/localhost），通常不会有跨域问题。部署到不同域名时需要配置。

---

## 六、当前 API 接口清单

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/` | 根路由 |
| GET | `/health` | 健康检查 |
| GET | `/api/news/categories` | 获取新闻分类列表 |
| GET | `/api/news/{id}` | 获取单条新闻 |

---

## 七、端口占用排查

如果端口被占用：

```bash
# Windows 查看端口占用
netstat -ano | findstr :8000
netstat -ano | findstr :5173

# 根据 PID 结束进程
taskkill /PID <PID> /F
```

---

## 八、一键启动脚本

创建 `start.bat` 放在项目根目录：

```bat
@echo off
echo ========== 启动后端 ==========
start "Backend" cmd /k "cd /d %~dp0backend && venv\Scripts\activate && uvicorn app.main:app --reload --host 127.0.0.1 --port 8000"

echo ========== 启动前端 ==========
start "Frontend" cmd /k "cd /d %~dp0frontend && npm run dev"

echo 后端: http://127.0.0.1:8000/docs
echo 前端: http://localhost:5173
```

---

## 九、常见问题速查

| 问题 | 原因 | 解决 |
|------|------|------|
| npm EPERM | 缓存目录权限/杀毒软件锁定 | 迁移缓存到 C 盘用户目录 |
| npm ENOTEMPTY | node_modules 目录损坏 | 用 cmd 手动删除整个目录 |
| 后端连不上 | MySQL 未启动或库不存在 | `CREATE DATABASE news_app` |
| API 500 错误 | 数据库表未创建 | 检查 ORM 模型与数据库表一致 |
| 前端页面空白 | vite 未启动或端口冲突 | 确认 `npm run dev` 输出正常 |
| CORS 错误 | 跨域未配置 | 添加 CORSMiddleware |


## 十、一些常见问题：

### 1. 跨域问题的说明
1. 跨域问题，前端和后端的域名或者端口号不一致，就会发生跨域问题。   
那么跨域问题是什么？简单来说，就是浏览器限制了同源策略，不允许跨域请求。  
跨域资源共享（cors）是一个机制，允许浏览器向跨域服务器发送请求。用于允许运行同一个源（origin）的网页，从另一个源的服务器请求数据。  
**前提是得经过服务器授权**  


### 2.跨域发生在客户端还是服务器？
一句话总结  
**跨域是浏览器"阻止"的，但解决需要服务器"同意"。**

| 步骤 | 操作方 | 说明 |
|:--:|:---|:---|
| 1 | 用户 | 访问 `http://localhost:5173`（前端页面） |
| 2 | 前端 JS | 发起请求到 `http://localhost:8000/api/news`（后端） |
| 3 | 浏览器 | 对比"协议/域名/端口"，发现不同 → 判定为**跨域** |
| 4 | 浏览器 | 自动在请求头添加 `Origin: http://localhost:5173` |
| 5 | 后端服务器 | 收到请求，决定是否允许： |
| | | • **允许**：返回响应头 `Access-Control-Allow-Origin: http://localhost:5173` |
| | | • **拒绝**：不返回该响应头 |
| 6 | 浏览器 | 检查响应头： |
| | | • 有 `Access-Control-Allow-Origin` 且匹配 → **放行，前端 JS 可读取响应** |
| | | • 无该头或不匹配 → **拦截，报 CORS 错误** |

### 关键角色

| 层面 | 作用 |
|:---|:---|
| **浏览器** | 执行安全拦截，检查响应头中的 `Access-Control-Allow-Origin` |
| **服务器** | 决定是否允许跨域，通过返回 CORS 响应头 |
### 重要特性

| 场景 | 是否触发跨域 |
|:---|:---|
| 浏览器前端 JS 请求 | ✅ 会触发 |
| `curl` 命令行 | ❌ 不会 |
| Postman | ❌ 不会 |
| 后端服务之间 HTTP 调用 | ❌ 不会 |

> **没有浏览器，就不存在跨域问题。**
---